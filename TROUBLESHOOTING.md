# トラブルシューティング / 開発メモ

入庫記録アプリで実際に起きた不具合と、その原因・解決方法の記録です。
**引き継ぎ用**なので、同じ問題が起きたときはここを最初に見てください。

---

## 目次
1. [QRが読み取れない（映像は出るが反応しない）](#1-qrが読み取れない映像は出るが反応しない)
2. [シリアルが「読取値のまま」入る（三菱QR）](#2-シリアルが読取値のまま入る三菱qr)
3. [アプリの更新が反映されない（古いまま）](#3-アプリの更新が反映されない古いまま)
4. [CSVが文字化けする](#4-csvが文字化けする)
5. [三菱製QRの仕様メモ](#5-三菱製qrの仕様メモ)
6. [よくある変更のやり方（型番の追加など）](#6-よくある変更のやり方型番の追加など)

---

## 1. QRが読み取れない（映像は出るが反応しない）

**症状**：カメラ映像は表示されるのに、QRに向けても何も起きない。スマホ純正のQRアプリでは読める。

**原因**：以前使っていた読取ライブラリ（ZXing）は、**ピンボケや低解像度のQRに弱い**。実際にボケた写真をZXingに通すと、前処理を変えても全滅した。一方スマホ純正は新しいML系エンジンなので読めていた。

**解決**：読取エンジンを切り替えた。
- **端末標準の `BarcodeDetector`（スマホ純正と同じGoogle製エンジン）を優先**。Android Chrome等で使え、高精度。
- 非対応端末（iPhone等）は従来の **ZXing に自動フォールバック**。
- あわせて、カメラを**高解像度要求＋連続オートフォーカス**に。
- **「写真で読み取る」ボタン**も追加（撮影画像はピントが合って高精細なので確実）。

該当コード：`app.js` の `getDetector()` / `startCamera()` / `scanLoop()` / `photoInput` ハンドラ。

---

## 2. シリアルが「読取値のまま」入る（三菱QR）

**症状**：三菱製QRを読むと、シリアル欄に本来の `2512312002A` ではなく、
`3096H94  2512312002096H94  202512313075JP096H94A` のような**読取値そのもの**が入ることが「時々」あった。

**原因**：QR内のフィールドの**区切り文字が、ふつうの空白ではなく制御文字（GS＝グループセパレータ、\x1D）**のことがあった。旧ロジックは「空白で区切る」前提だったため、区切りに失敗し、シリアルを取り出せず読取値をそのまま入れていた。
- チャットに貼ると制御文字が空白に変換されるため、**画面上は正常に見え、再現しにくかった**。これが「時々」の正体。

**解決**：区切り文字に依存しない方式へ変更。
- 英数字以外（空白・制御文字・記号）を**すべて除去して連結**。
- **3回繰り返す品目コード**を手がかりにシリアルを抽出。
- 空白／GS／区切り無し、いずれでも動作し、一般的なQR・URL・JSONには誤反応しない（テスト済み）。

該当コード：`app.js` の `parseMitsubishi()` / `findRepeatedToken()`。

> **今後、別フォーマットで同じ症状が出たら**：その箱の型番と、画面下の「読取値」を控える。読取値さえ分かれば、同じ要領で抽出ロジックを追加できる。

---

## 3. アプリの更新が反映されない（古いまま）

**症状**：コードを更新したのに、スマホのアプリが古いまま。

**原因**：Service Worker（オフライン用の仕組み）が**キャッシュ優先**だったため、保存済みの古いファイルを使い続けていた。

**解決**：Service Worker を**ネットワーク優先**に変更（`sw.js`）。
- オンライン時：常に最新を取得してキャッシュ更新。
- オフライン時：保存版を使用（現場でネット無しでも動く）。

**それでも古い場合の対処**（利用者向け）：
1. アプリを**完全に閉じて開き直す**。
2. 直らなければ**アンインストール → Chromeでサイトのデータ削除 → URLを開いて入れ直し**。

---

## 4. CSVが文字化けする

**症状**：書き出したCSVを開くと日本語（ヘッダー等）が化ける。

**原因**：ファイルは **UTF-8（BOM付き）で正しい**。化けるのは**開く側**の問題で、特に**スマホの簡易ビューア**や一部ソフトが UTF-8 を Shift-JIS と誤認するため。

**解決（開き方）**：
- スマホで中身確認 → アプリの**「記録一覧」タブ**を見る（化けない）。
- スマホで表として見る → **Googleスプレッドシートで開く**（UTF-8対応）。
- PCのExcel → **データ → テキストまたはCSVから → 文字コード 65001:UTF-8 → 読み込み**。
- **Notion / Supabase 取り込みでは化けない**（UTF-8前提のため）。

> **Shift-JISには変えない方針**。変えるとNotion/Supabase/Googleスプレッドシートで逆に化けるため、横展開を考えると UTF-8 のままが正解。

該当コード：`app.js` の CSV書出処理（先頭に BOM `﻿` を付与）。

---

## 5. 三菱製QRの仕様メモ

実物のQRをデコードすると、次の並びになっている（区切りは空白または制御文字GS）。

```
[先頭]  [品目コード]   [シリアル基幹][品目コード]   [日付+075JP][品目コード][区分]
  3        096H94        260312002 096H94        20260313075JP 096H94  A
```

- **品目コード**（例 `096H94`）… 製品ごとに異なる。QR内に**3回繰り返す**。
- **シリアル** … 「シリアル基幹（数字9〜10桁）」＋「末尾の区分英字」。
  - 例：`260312002` ＋ `A` → **`260312002A`**（これが台帳のシリアル）
- **日付**（例 `20260313` = 2026/03/13）… おそらく製造日。**入庫日付には使わない**（入庫日＝受け入れた当日を既定にする）。
- **先頭の `3`** … 用途不明。無視してよい。
- **型番（EF-40DTC2-V 等）はQRに入っていない** → プルダウンで手動選択する。

抽出方針：品目コードの**値に依存せず**、「3回繰り返す部分＝コード」という**構造**でシリアルを取り出している（コードが製品ごとに変わっても動く）。

---

## 6. よくある変更のやり方（型番の追加など）

- **型番リストの増減** … `app.js` 冒頭の `const MODELS = [ ... ]` に型番を追加/削除するだけ。シリーズ分け（EF/EG…）は先頭の記号から自動。
- **公開URL** … `https://kaz0520.github.io/motor-serial-number-check/`
- **更新の流れ** … コードを push → GitHub Actions が自動で GitHub Pages に公開（`.github/workflows/deploy-pages.yml`）。
- **データの保存先** … 各端末内（localStorage）。外部送信なし。集約は CSV書出 → Notion/Supabase 取り込み。

---

_このメモは不具合対応のたびに追記してください。_
